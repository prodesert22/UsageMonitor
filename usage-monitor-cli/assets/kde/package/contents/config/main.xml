<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
                          http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
  <!-- The widget stores its settings in the helper's state.json, not KConfig.
       The config pages drive the native Apply/OK via `signal configurationChanged`
       + `function saveConfig()` (see contents/ui/config*.qml). This file only
       needs to exist for the config dialog to appear. -->
  <kcfgfile name=""/>
  <group name="General">
  </group>
</kcfg>
