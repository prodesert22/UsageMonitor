# v0.7.1

Polishes the KDE widget text and chooser icon introduced around the built-in
widget installer work.

## Changed

- Workspace crate version and KDE plasmoid metadata were bumped to `0.7.1`.

## Fixed

- Widget reset descriptions are now capitalized at the Rust payload layer (for
  example, `Resets at HH:MM`), so the KDE UI no longer renders duplicate text
  like `Resets: resets ...`.
- The KDE widget installer now registers the Usage Monitor icon in the user
  hicolor theme and the plasmoid metadata references it, so Plasma's widget
  chooser shows the project icon instead of the stock monitor icon.

## Install

```bash
cargo install usage-monitor-cli
usage-monitor-cli widget install kde
```

## Compare

[`v0.7.0...v0.7.1`](https://github.com/prodesert22/UsageMonitor/compare/v0.7.0...v0.7.1)
